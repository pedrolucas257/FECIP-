//desenhando quadrados
for (var i = 0; i < linhas; i++)
{
	for (var j = 0; j < colunas; i++)
	{
		
		if img > img_num{img = img_num}
		
		//garantindo que ele comece da primeira coluna 
		var _img = min(max(0, img - j), img_num)
		
		
		
		draw_sprite_ext(spr_transicao,img, j * tamanho, i * tamanho,1,1,0,c_black,1)
	}
}